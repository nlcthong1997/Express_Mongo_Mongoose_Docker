{
  "name": "app",
  "version": "1.0.0",
  "description": "Express Mongo Redis Docker",
  "scripts": {
    "start": "npx tsc && cp $(pwd)/.env $(pwd)/dist/.env && cd $(pwd)/dist/api && node index.js",
    "build": "npx tsc && cp $(pwd)/.env $(pwd)/dist/.env",
    "dev": "cd $(pwd)/api && nodemon -L -T index.ts",
    "test": "echo \"Test scripts\" && exit 1"
  },
  "author": "Thong Nguyen",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.20"
  },
  "dependencies": {
    "@types/express": "^4.17.16",
    "@types/node": "^18.11.18",
    "bcrypt": "^5.1.0",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-async-errors": "^3.1.1",
    "express-async-handler": "^1.2.0",
    "jsonwebtoken": "^9.0.0",
    "mongoose": "^6.9.0",
    "mongoose-unique-validator": "^3.1.0",
    "slugify": "^1.6.5",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5",
    "winston": "^3.8.2"
  }
}
